"use client"

import { useScrollTop } from "@/hooks/use-scroll-top"
import { cn } from "@/lib/utils"

export const Navbar = () => {
    const scrolled = useScrollTop()

    return (
        <div>
            
        </div>
    )
}